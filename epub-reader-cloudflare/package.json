{
  "name": "epub-reader-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute epub-reader-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute epub-reader-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "fast-xml-parser": "^4.5.0",
    "itty-router": "^4.2.2"
  }
}
